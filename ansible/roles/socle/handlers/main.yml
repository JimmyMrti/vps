---
- name: Recharger sysctl
  ansible.builtin.command: sysctl --system
  changed_when: true

- name: Recharger systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Reconstruire l'initramfs
  ansible.builtin.command: update-initramfs -u
  changed_when: true
